# Backend

The deployable FastAPI service lives in [`retrieve_backend/`](retrieve_backend/).

That folder is self-contained for deployment and includes its own:

- `requirements.txt`
- `.env.example`
- `README.md`
- FastAPI source files

For setup, local run commands, environment variables, and deployment instructions, see [`retrieve_backend/README.md`](retrieve_backend/README.md).
